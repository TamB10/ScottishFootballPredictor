name: Update Stats

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Create stats directory
        run: mkdir -p stats

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build and Run Scraper with Debug Info
        run: ./gradlew runScraper --stacktrace --info --debug

      - name: Commit and push if changes
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add stats/* || true
          git commit -m "Update stats data" || true
          git push || true